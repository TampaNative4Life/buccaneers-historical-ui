# ===============================================================
# FILE: .github/workflows/pages.yml
# PROJECT: Buccaneers Historical Database UI
#
# FILE STATUS
# -----------
# Initial working deployment workflow
#
# CREATED
# -------
# 2026-03-14
#
# PURPOSE
# -------
# Deploy GitHub Pages from /site folder
#
# STRUCTURE
# ---------
# 1. Checkout repo
# 2. Configure GitHub Pages
# 3. Upload /site folder
# 4. Deploy to Pages
#
# ===============================================================

name: Deploy site folder to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
